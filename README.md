Steps to run the code:
open terminal and go to the  backend dir and run these two commands:
 1) npm install
 2) npm start

 open new terminal and go to the frontend dir and again run these two command
  1) npm install
 2) npm start

 back end port listen to 8081 (make sure to check which port your code listens up!!!)